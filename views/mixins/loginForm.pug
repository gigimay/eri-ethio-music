mixin loginForm()
  form.form(action="/login" method="POST")
    h2= title
    label(for="email") input ur email
    input(type="email" name="email")
    label(for="password") password
    input(type="password" name="password")
    input.button(type="submit" value=" Log In -<")
